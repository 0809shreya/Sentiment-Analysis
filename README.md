# Sentiment-Analysis
This project builds a Python text classification pipeline. NLTK is used for text preprocessing, TF-IDF feature extraction is done with Scikit-learn, and a Random Forest classifier is trained with `GridSearchCV`. Performance is evaluated using F1 score, accuracy, and more.
